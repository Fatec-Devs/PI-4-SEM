{
  "name": "lambda-password-rotation",
  "version": "1.0.0",
  "description": "Lambda function for automatic password rotation",
  "main": "password-rotation.js",
  "scripts": {
    "build": "tsc",
    "package": "npm run build && zip -r function.zip dist node_modules package.json"
  },
  "dependencies": {
    "@aws-sdk/client-secrets-manager": "^3.700.0",
    "@prisma/client": "6.6.0",
    "pg": "^8.16.3"
  },
  "devDependencies": {
    "@types/aws-lambda": "^8.10.145",
    "@types/node": "^20.19.25",
    "prisma": "6.6.0",
    "typescript": "^5.9.3"
  }
}
